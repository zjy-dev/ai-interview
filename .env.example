# 敏感配置 - 不要提交到版本控制
# 复制此文件为 .env 并填入实际值

# ── 本地开发 (make dev) ──────────────────────────────
# 本地开发时后端直连宿主机上的 MySQL/Redis
# Docker Desktop (macOS/Windows) 和 docker compose 通常 127.0.0.1 可用
# Rootless Podman 端口映射可能不生效，需要改用容器 IP：
#   podman inspect ai-interview-mysql --format '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'
DB_DSN=root:password@tcp(127.0.0.1:3306)/ai_interview?parseTime=true&loc=Local
REDIS_ADDR=127.0.0.1:6379
REDIS_PASSWORD=

# ── 容器部署时这两项会被 docker-compose.yml 的 environment 覆盖 ──
# DB_DSN → root:password@tcp(mysql:3306)/ai_interview?...
# REDIS_ADDR → redis:6379

# ── 通用 ────────────────────────────────────────────
# JWT 密钥 (生产环境请更换为强随机字符串)
JWT_SECRET=your-jwt-secret-change-me

# 加密密钥 (用于 AES 加密存储用户的 BYOK API Key, 64 位 hex = 32 bytes)
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
